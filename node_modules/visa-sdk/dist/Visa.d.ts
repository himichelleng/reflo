import * as rest from './utils/rest';
export interface VisaApiCredentials {
    userId: string;
    password: string;
    certificate_file_path: string;
    certificate_passphrase: string;
}
export declare enum Endpoint {
    Sandbox = "https://sandbox.api.visa.com",
    Certification = "https://cert.api.visa.com",
    Production = "https://api.visa.com"
}
export declare class Visa {
    private _credentials;
    private _host;
    constructor({ userId, password, certificate_passphrase, certificate_file_path }: VisaApiCredentials, host?: Endpoint);
    protected get(uri: string, options?: rest.options): Promise<{
        response: import("request").Response;
        body: {};
    }>;
    protected delete(uri: string, options?: rest.options): Promise<{
        response: import("request").Response;
        body: {};
    }>;
    protected put(uri: string, body: any, options?: rest.options): Promise<{
        response: import("request").Response;
        body: any;
    }>;
    protected post(uri: string, body: any, options?: rest.options): Promise<{
        response: import("request").Response;
        body: any;
    }>;
    helloWorld(): Promise<{
        response: import("request").Response;
        body: {};
    }>;
}
//# sourceMappingURL=Visa.d.ts.map